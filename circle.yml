## Customize dependencies
# dependencies:
#   :pre

#     - mkdir -p circleci/junit

## Customize test commands
test:
  override:
    - bundle exec rspec -r rspec-extra-formatters -f JUnitFormatter -o circleci/junit/rspec.xml
  # post:
  #   - bundle exec rake jasmine:ci: # add an extra test type
  #       environment:
  #         RAILS_ENV=test
  #         RACK_ENV=test

