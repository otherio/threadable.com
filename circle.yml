machine:
  environment:
    RACK_ENV: test
    HONEYBADGER_API_KEY: FAKE_HONEYBADGER_API_KEY
    SELENIUM_BROWSER: firefox

test:
  override:
    - script/ci-parallel:
      timeout: 200
      parallel: true

deployment:
  staging:
    branch: master
    commands:
      - script/deploy staging
