machine:
  environment:
    RACK_ENV: test
    THREADABLE_FILEPICKER_API_KEY: FAKE_THREADABLE_FILEPICKER_API_KEY
    THREADABLE_S3_ACCESS_KEY_ID: FAKE_THREADABLE_S3_ACCESS_KEY_ID
    THREADABLE_S3_SECRET_ACCESS_KEY: FAKE_THREADABLE_S3_SECRET_ACCESS_KEY
    THREADABLE_S3_BUCKET_NAME: FAKE_THREADABLE_S3_BUCKET_NAME
    HONEYBADGER_API_KEY: FAKE_HONEYBADGER_API_KEY

test:
  override:
    - script/ci-build: {timeout: 200}

deployment:
  staging:
    branch: master
    commands:
      - script/deploy staging
