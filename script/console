#!/usr/bin/env ruby

case ARGV.first
when 'staging'
  exec 'heroku run console --app covered-staging'
when 'production'
  exec 'heroku run console --app covered-production'
when 'local'
  exec 'bundle exec rails c'
else
  exec 'bundle exec rails c'
end


