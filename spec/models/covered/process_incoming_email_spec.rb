require 'spec_helper'

describe Covered::ProcessIncomingEmail do


  it "should have tests"

  # it "should call through to Covered::ProcessIncomingEmail::CreateConversationMessage" do
  #   incoming_email = double(:incoming_email)
  #   expect(Covered::ProcessIncomingEmail::CreateConversationMessage).to receive(:call).with(covered, incoming_email)
  #   described_class.call(covered, incoming_email)
  # end
end
