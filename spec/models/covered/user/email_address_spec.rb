require 'spec_helper'

describe Covered::User::EmailAddress do



  describe 'primary!' do
    context 'when the email address is already primary' do
      it 'just returns' do

      end
    end
    context 'when the email address is already primary' do
    end
  end


end
