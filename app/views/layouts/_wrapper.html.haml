!!!
%html
  %head
    %title Covered
    = stylesheet_link_tag    'old', :media => "all"
    = csrf_meta_tags
    = javascript_include_tag 'old'

  %body

    .page
      .content
        = yield
      .flash_messages
        - flash.each do |name, msg|
          = content_tag :div, msg, class: "alert alert-#{name}"

