<%= @message.body_plain %>


_____
<% if @conversation.groups.count > 0 %>
In <%= "#{'Group'.pluralize(@conversation.groups.count)}" %>:
<% @conversation.groups.all.each do |group| %>
  +<%= group.name %>: <%= group.email_address %>
<% end %>

<% end %>
View on Threadable:
<%= @message_url %>

Start a new conversation:
<%= mail_to_href @organization.email_address %>

Start a new task:
<%= mail_to_href @organization.task_email_address %>

Unsubscribe:
<%= @unsubscribe_url %>

Feedback:
<%= mail_to_href @threadable.support_email_address %>
