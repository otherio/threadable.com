= simple_form_for @project do |f|

  = f.error_notification

  = f.input :name
  = f.input :slug
  = f.input :subject_tag, input_html:{value: @project.subject_tag}
  = f.input :description
  = f.submit 'Save', class: 'btn btn-primary'
