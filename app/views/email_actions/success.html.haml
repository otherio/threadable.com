.uk-container-center.uk-width-large-1-3.uk-width-medium-1-2

  %h3 #{@email_action.success_description}

  %h3 Remember:
  .well-when-not-small
    %p
      You can #{@email_action.pending_description} via email! Just reply to the conversation and add
      %br/
      - case @email_action.type
      - when 'add', 'remove'
        %strong "&#{@email_action.type} #{@email_action.user ? @email_action.user.name : 'Your Name'}"
      - else
        %strong "&#{@email_action.type}"
      %br/
      to the top of your response.
