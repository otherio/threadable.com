
- if signed_in?

  - @task = current_user.tasks.first || current_user.projects.first.tasks.first
  - @project = @task.project

  = render_widget :task_metadata, task: @task, user: current_user

- else

  %h2 please sign in to view this widget
